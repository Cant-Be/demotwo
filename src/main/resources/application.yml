
server:
  port: 8081
  servlet:
    context-path: /
spring:
  datasource:
#    数据库路径                                      （？）
    url: jdbc:postgres://localhost:5432/zydemo
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

mybatis:
  mapper-locations: classpath:mapper/**/*.xml